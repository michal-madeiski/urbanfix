namespace UrbanFix.Common
{
    public enum VerificationDecision
    {
        Pending,
        Accepted,
        Rejected
    }

    public enum TaskAssignmentStatus
    {
        New,
        InProgress,
        Completed,
        Rejected
    }

    public enum ReportStatus
    {
        New,
        Verified,
        Rejected,
        Assigned,
        Completed
    }
}
